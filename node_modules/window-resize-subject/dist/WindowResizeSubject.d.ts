import type { Subject, WindowResizeObserver, WindowResizeSubjectEvent, ObserverName, WindowResizeSubjectOption } from './type';
export declare class WindowResizeSubject implements Subject<WindowResizeSubjectEvent> {
    private _observers;
    private _delay;
    private _timer;
    private _subscribed;
    private _handler;
    constructor(option?: WindowResizeSubjectOption);
    addObserver(name: ObserverName, observer: WindowResizeObserver): this;
    deleteObserver(name: ObserverName): this;
    deleteObservers(): this;
    notifyObservers(event: WindowResizeSubjectEvent): this;
    subscribe(): this;
    unsubscribe(): this;
    setDelay(delay: number): this;
    hasObserver(): boolean;
    private _getEvent;
    private _update;
    private _handleResize;
}
